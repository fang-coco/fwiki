## 核心写法

- 文档按**源码主线**来写，顺着调用链、执行顺序或数据流往下展开，不要东一块西一块地拆散说明。
- 采用**一段代码 + 紧跟一段解释**的形式，不要先堆很多代码，再在后面集中写一大段解释。
- 每一段除了说明“这里做了什么”，还要明确写出**这一步产出了哪些关键数据、状态或结果，后面交给谁继续处理**。
- 代码块外写**整体结论、上下文位置、主线关系**。
- 代码块内写**具体动作**，用中文注释说明这一行或这一段到底做了什么。
- 如果某段代码只是为了帮助理解主线，可以适当删去不关键的实现，只保留关键路径。

## 代码块的要求

- 代码中的中文注释要解释**具体行为**，例如：
  - 为什么进入这个分支
  - 这里初始化了什么状态
  - 这里为什么要清零/恢复/挂起
  - 这里把哪些对象加入了下一轮处理队列
- 不要把所有细节都放到代码块外面讲。
- 代码块中的注释优先解释“**具体做了什么**”，而不是空泛复述函数名。
- 注释要有**连接性**，不要只逐句翻译当前行的语义。每一条较长的注释都应体现三点之一：**当前代码处在主线的哪一步、前一步产出了什么、这一步做完后会交给谁**。避免出现“这一行调用了 X 函数”“这里 for 循环遍历数组”这种只是复述语法的注释。
- 允许对原始代码做适度精简，但不能改掉主线语义。
- 优先保留**控制流、关键分支、关键数据写入和关键函数调用**。
- 精简的目标是"去噪"，不是"去主线"。以下几类属于可以整体折叠成一句注释或直接省略的边界噪音：**dormancy hysteresis、Paused/Reliable ACK 等待、Checkpoint / Replay / ResendAllData 补发分支、CSV/Profiler/Trace 采样、debug 日志、STATS 计时、预准备变量、保护性 ensureMsgf、Push Model 验证支路** 等。它们如果不是当前主线要讲的对象，就不必逐条展开，避免代码块被噪音撑爆。
- 精简不等于用 `...` 大段跳过关键实现。如果读者需要看到具体字段（例如 `SharedParams` 的实际组成、`RepFlags` 的组装内容），要展开写清楚，不要用 `...` 省略。
- 如果代码块已经在注释中写清了具体动作，代码块外不要再机械复述一遍。

## 代码块外文字的要求

- 代码块外不要频繁使用“这一段的作用”“这一段的全局作用”“这一部分在做什么”这类提示性标签。
- 代码块外直接写结论，按**叙述文、记录文**的方式自然往下写。
- 文字应像技术笔记，而不是讲义式拆分。
- 每段解释都要说明它和上一步、下一步之间的关系，让整篇文档能顺着读下去。
- 优先写清楚**当前函数/代码段的输入、输出、状态变化，以及这些结果如何流向后续函数**。
- **潜在问题、易错点、边界陷阱**要作为**一句话或半段文字直接融入当前函数的结论段末尾**，不要单独立小标题、不要再另贴一份对照代码块。写法通常是"需要留意的是...""这里的一个易错点是...""这也带出一个潜在问题：..."等直接接续原结论的句式。
- 不写"这一层小结""本节小结""这一节的作用是..."式的独立收尾节。整篇文档的收尾也应该融进最后一段主线叙述里，而不是另起一节做总结。

## 结构要求

- 标题层级**最多三级**。
- 三级小标题采用 **`函数签名 + 简短说明`** 的形式，例如 `### FRepLayout::CompareProperties(...) 把差异写成一条 changelist`。这样读者看到小标题就能定位到源码位置，同时知道这一节负责主线的哪一步。
- 小标题不要过多，避免把文档切得太碎。
- 优先保证主线连续，而不是把“参数、分支、配置、总结”拆成很多独立小节。
- 如果参数、配置、分支判断确实重要，应直接融合到对应代码段后面的解释里。
- 文档开头需要一段**高层概览**，用几句话说清整条主线分几层、每层负责什么、上下层怎么衔接。开头段之后再进入第一个三级标题。

## 图表要求

- 图表的目标是帮助读者看清**调用顺序、数据流向、阶段边界**，不要为了画图而画图。
- 需要表现跨函数调用链和数据交接时，优先使用**时序图**。
- 只有在确实需要强调判断分支、状态跳转或并行分叉时，再考虑流程图。
- 图中的节点和箭头名称应直接对应当前实现中的函数、数据结构或状态，不要写抽象空话。

## 内容要求

- 必须基于**当前版本代码实现**写，不要凭旧印象或历史版本经验泛泛总结。
- 如果某个判断条件、标志位、数据结构在当前版本有变化，文档里要按当前实现记录。
- 对关键分支要写清楚：
  - 分支为什么成立
  - 分支成立后实际走到哪里
  - full / incremental、初始化 / 恢复、根集 / 扩散等差异分别体现在哪
- 对关键数据结构要写清楚它们在主线中的职责，例如初始对象列表、待处理队列、根集、cluster 结果集合等。
- 如果某个函数本身不复杂，但它产出的结果决定了后面怎么走，也要把这个“交接边界”写清楚。

## 表达风格

- 少写空话，直接下结论。
- 少写模板化总结句，优先写“发生了什么、为什么这样做、接下来会去哪里”。
- 行文风格保持统一，避免一会儿像注释，一会儿像讲义，一会儿像摘要。
- 目标效果是：读者顺着文档往下看，就能像顺着源码单步调试一样理解主线。

## 推荐检查清单

写完一节后，检查以下几点：

- 是否做到了“一段代码 + 一段解释”。
- 代码中的中文注释是否解释了具体动作，是否体现了"上一步 → 当前步 → 下一步"的连接性，而不是逐句翻译。
- 代码块里是否残留了 dormancy hysteresis、Paused ACK、Checkpoint 补发、CSV/Profiler/Trace、debug 日志等边界噪音；这些如果不是当前主线的对象，应折叠成一句注释或直接省略。
- 代码块里是否用 `...` 跳过了本该展开的关键字段（例如结构体的实际组成）。
- 代码块外是否仍然残留大量“这一段的作用”式表达。
- 三级小标题是否采用了 `函数签名 + 简短说明` 的格式。
- 标题是否超过三级。
- 小标题是否过多、是否打断主线。
- 潜在问题 / 易错点 / 边界陷阱是否融进了对应结论段落，而不是单独起一小节。
- 是否写了"这一层小结""本节小结"这类独立收尾节；如果有，应把内容合并进最后一段主线叙述。
- 文档开头是否有一段高层概览，读者能否在进入第一个三级标题前先看清整条主线。
- 是否基于当前实现，而不是泛泛而谈。
- 是否能让人顺着文档一路读下去，而不需要来回跳。
- 是否写清楚了每一步产出的关键数据，以及这些数据后面流向哪里。
- 图表是否真的帮助理解主线；如果只是补充调用与数据交接，是否更适合改成时序图。